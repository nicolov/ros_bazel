package(default_visibility = ["//visibility:public"])

# TODO: header files are somehow found even when not declared, fix this.
cc_library(
    name='bz2',
    # hdrs=glob(['glog/*.h']),
    srcs=[
        'lib/x86_64-linux-gnu/libbz2.a',
    ],
    licenses = ["notice"],
)
